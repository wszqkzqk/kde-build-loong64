#!/bin/bash

. common

mkdir -p build
cd build

for i in ${depPkgName} ${optPkgName}; do
  if [ ! -d $i ]; then
    pkgctl repo clone $i
  fi
  _pushd $i
    git pull origin ${gitbranch}
  _popd
done
