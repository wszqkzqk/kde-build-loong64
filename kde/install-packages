#!/bin/bash

. common

pkglist=''

for i in `cat kde-dep-packages kde-opt-packages`; do
	_pushd build/${i}/${svnbranch}
	unset pkgname
	. PKGBUILD
	for j in ${pkgname[@]}; do
		pkgfile=${j}-${pkgver}-${pkgrel}-${CARCH}${PKGEXT}
		pkglist="${pkglist} ${pkgfile}"
	done
	_popd
done


#for i in ${l10nPkgName}; do
#	pkglist="${pkglist} build/${i}/${svnbranch}/${i}-*.pkg.tar.gz"
#done


sudo pacman -U ${pkglist}
