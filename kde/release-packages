#!/bin/bash

. common

arch=$1
[[ -z ${arch} ]] && arch=${CARCH}

for i in ${depPkgName} ${optPkgName}; do
	_pushd build/${i}/$svnbranch
	if [ "$i" == "oxygen-icons" -a "${any}" == "true" ] || [ "${l10n}" == "true" ]; then
		archrelease ${repo}-any
	else
		archrelease ${repo}-${arch}
	fi
	_popd
done