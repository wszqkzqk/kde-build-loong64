#!/bin/bash

. common

for i in ${depPkgName} ${optPkgName} kde-meta; do
	_pushd build/${i}/${svnbranch}
	rm -rf pkg src *.log* *${PKGEXT} *.part *~ *.sig
	_popd
done

if ${l10n}; then
	_pushd build/kde-l10n/${svnbranch}
	rm -rf pkg src *.log* *${PKGEXT} *.part *~ *.sig
	_popd
fi
