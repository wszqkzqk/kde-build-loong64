#!/bin/bash

. common

_pushd build

for i in ${depPkgName} ${optPkgName}; do
	_pushd ${i}/${svnbranch}
	rm -rf pkg src *.log* *${PKGEXT} *.part
	_popd
done

if ${l10n}; then
	_pushd kde-l10n/${svnbranch}
	rm -rf pkg src *.log* *${PKGEXT} *.part
	_popd
fi

_popd
